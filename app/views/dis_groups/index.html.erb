<p id="notice"><%= notice %></p>

<h1>Listing Dis Groups</h1>

<table>
  <thead>
    <tr>
      <th>Dis name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dis_groups.each do |dis_group| %>
      <tr>
        <td><%= dis_group.disname %></td>
        <td><%= link_to 'Show', dis_group %></td>
        <td><%= link_to 'Edit', edit_dis_group_path(dis_group) %></td>
        <td><%= link_to 'Destroy', dis_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dis group', new_dis_group_path %>
