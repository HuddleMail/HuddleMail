<h1>New Distribution Group</h1>

<%= render 'form' %>

  <% if @dis_group.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@dis_group.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @dis_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= link_to 'Back', dis_groups_path %>
