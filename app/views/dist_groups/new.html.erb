<h1>New Distribution Group</h1>

<%= render 'form' %>

  <% if @dist_group.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@dist_group.errors.count, "error") %> prohibited
        this group from being saved:
      </h2>
      <ul>
        <% @dist_group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= link_to 'Back', dist_groups_path %>
